/**
 * TEMPORARY dev-only page — triggers the global error boundary so you can
 * preview the branded crash screen without breaking anything.
 * Delete this file when done reviewing.
 */
export default function ErrorPreviewPage() {
  throw new Error('Preview: this is a test error to show the error boundary UI.')
}
